{
  "name": "ceo_web_project_v1",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "dev": "pnpm --filter app dev",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "format": "prettier --write .",
    "lint:prettier:fix": "prettier --write .",
    "lint:eslint:fix": "pnpm --filter app exec eslint --fix .",
    "test:tsc:filtered": "pnpm --filter app exec tsc --noEmit",
    "test:regular": "pnpm --filter app run test",
    "test:utilities": "echo 'No utility tests yet' && exit 0"
  },
  "devDependencies": {
    "husky": "^9.0.11",
    "lint-staged": "^15.2.2",
    "@commitlint/cli": "^19.0.3",
    "@commitlint/config-conventional": "^19.0.3",
    "prettier": "^3.2.5",
    "typescript": "^5.3.3"
  },
  "lint-staged": {
    ".app/**/*.{ts,tsx,js,jsx}": [
      "pnpm --filter app exec eslint --fix",
      "prettier --write"
    ],
    "**/*.{md,json}": [
      "prettier --write --ignore-unknown"
    ]
  }
}
